# Aadrik AI backend dependencies
# Curated from the original pip freeze - trimmed to packages actually
# imported in app/. See audit notes for what was removed and why.

fastapi==0.138.1
uvicorn[standard]==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2

# Chat + embeddings client (local LM Studio, zero cost - see app/core/config.py)
openai==2.44.0

# RAG / vector store
chromadb==1.5.9
langchain-community==0.4.2
langchain-core==1.4.8
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2

# Document loaders
pypdf==6.14.2
python-docx==1.2.0

# File uploads (Knowledge Base Manager) - FastAPI's UploadFile/Form need this
# to parse multipart/form-data; it's a peer dependency, not auto-installed.
python-multipart==0.0.32

# Quotation PDF generation (app/services/quotation_pdf.py) - Pillow is
# needed for the JPEG logo embedded in the header.
reportlab==5.0.0
Pillow==12.3.0

# Auth (password hashing + JWT) - see app/core/security.py
# NOTE: bcrypt must stay <4.1 - passlib 1.7.4 reads bcrypt.__about__.__version__,
# which was removed in bcrypt 4.1+, breaking hash_password() at runtime.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose==3.5.0
email-validator==2.3.0

